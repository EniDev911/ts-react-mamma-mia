export interface IPizza {
    desc: string,
    id: string,
    img: string,
    ingredients: string[],
    name: string,
    price: number
}

export interface InterfaceCarrito {
    id: string,
    cantidad: number,
    total: number
}

